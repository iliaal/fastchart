# nayuki/QR-Code-generator — vendored under vendor/qrcodegen/ for the
# FastChart\QrCode encoder. C variant (qrcodegen.c + qrcodegen.h),
# MIT licensed. The freshness gate compares the pinned commit against
# upstream HEAD; bump `pinned` here when re-syncing the vendor copy.

# Upstream publishes release tags years behind the C code everyone
# vendors, and the vendor directory mirrors a commit rather than a
# release — compare against branch HEAD, not releases/latest.
resolver: github-head
repo: nayuki/QR-Code-generator
branch: master
pinned: 993ccbb6765327f5c49939ba77736098c04ab81d
vendored_at: vendor/qrcodegen
note: >-
  Re-synced to master 993ccbb (2026-08). The 6 commits since 2c9044d touch
  only the fast-Java implementation and the Readme; c/qrcodegen.{c,h} are
  byte-identical to the prior pin, so the vendor copy already matches head.
  The vendored qrcodegen.c additionally carries a fastchart-local perf patch
  (GF(2^8) log/antilog tables in initReedSolomonTables replacing the
  upstream Russian-peasant multiply); qrcodegen.h is upstream-verbatim.
